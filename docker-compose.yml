services:
  app:
    build: .
    ports:
      - "5885:5885"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - WATCHPACK_POLLING=true

volumes:
  node_modules:
